# Convex Backend

Run `bunx convex dev` to initialize or attach this repository to a Convex project.

The frontend client reads `VITE_CONVEX_URL` and wires Convex through `ConvexProvider` in `src/main.tsx`.
